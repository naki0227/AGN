{
    "version": 2,
    "builds": [
        {
            "src": "web/package.json",
            "use": "@vercel/next",
            "config": {
                "buildCommand": "cd .. && ./build_wasm.sh && cd web && npm run build"
            }
        }
    ]
}